import type { RetrievalEvidence } from '../types/reviewTask';
export function RetrievalEvidencePanel({items,loading,error}:{items:RetrievalEvidence[];loading?:boolean;error?:string|null}) { if(loading)return <p>Loading evidence…</p>; if(error)return <p role="alert">{error}</p>; if(!items.length)return <p>No evidence available.</p>; return <div>{items.map(item=><div key={`${item.citationLabel}-${item.path}-${item.startLine}`}><strong>{item.citationLabel}</strong> <span>{item.path}:{item.startLine}-{item.endLine}</span><p>{item.excerpt}</p><small>Rank {item.rank} · Score {item.score}</small></div>)}</div>; }
