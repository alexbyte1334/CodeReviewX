import type { RepositoryIndexStatus as Status } from '../types/reviewTask';
export function RepositoryIndexStatus({status,onIndex,onReindex}:{status?:Status|null;onIndex?:()=>void;onReindex?:()=>void}) { const value=status?.status ?? 'NOT_INDEXED'; const label=value==='INDEXING'?'Indexing':value[0]+value.slice(1).toLowerCase().replace('_',' '); return <div className="status-widget"><span>{label}</span>{value==='NOT_INDEXED'&&onIndex&&<button type="button" onClick={onIndex}>Index</button>}{(value==='FAILED'||value==='DEGRADED'||value==='READY')&&onReindex&&<button type="button" onClick={onReindex}>Reindex</button>}</div>; }
