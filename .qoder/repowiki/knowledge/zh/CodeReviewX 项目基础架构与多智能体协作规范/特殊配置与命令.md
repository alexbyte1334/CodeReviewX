- **环境初始化**：需复制 `.env.example` 为 `.env` 并填充 `GITHUB_TOKEN` 和 `LLM_API_KEY`。
- **分阶段开发**：当前处于 Round 01（仓库基础），无业务代码；后续轮次需遵循 `tasks/round-NN/` 中的任务指引。
- **智能体交接**：每轮开发需生成 `handoff/round-NN/` 报告，经架构师审核后方可进入下一环节。